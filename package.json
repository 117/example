{
  "name": "example",
  "version": "0.0.0",
  "description": "an example typescript project",
  "main": "./dist/main.js",
  "author": "117",
  "license": "ISC",
  "scripts": {
    "build": "rimraf dist && npm i && tsc"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
